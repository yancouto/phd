[package]
name = "dynamic_2core"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4"
rand = "0.8"
derivative = "2"
debug_tree = "0.4"

[dev-dependencies]
flexi_logger = { version = "0.29", default-features = false, features = [
    "colors",
] }
